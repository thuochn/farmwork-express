h1 Books
h4
	a(href="book/create") CreateBook
	br
	a(href="/users") UserBooks
form(action= "/search", method= "GET")
	input(type= "text", placeholder= "Search Books", name= "q")
	button search
each product in products
	li= product.title
	a(href="/view/" + product.id) xem chi tiet